# راهنمای دیپلوی Railway — ربات فضول‌یاب (نسخه ۳)

## ✅ متغیرهای محیطی (Railway → سرویس fozol-bot → Variables)

ربات برای اجرا به این ۴ متغیر نیاز دارد. **هیچ مقداری در کد هاردکد نشده** — بعد از دیپلوی، در Railway → Variables این ۴ متغیر را بسازید و مقادیر را وارد کنید:

| متغیر | وضعیت اولیه | توضیح |
|---|---|---|
| `BOT_TOKEN` | ❗ بعد از دیپلوی پر کنید | توکن ربات بله |
| `ADMIN_ID` | ❗ بعد از دیپلوی پر کنید | شناسه عددی سوپر ادمین |
| `PROVIDER_TOKEN` | ❗ بعد از دیپلوی پر کنید | توکن درگاه پرداخت |
| `DB_PATH` | `/data/bot_data.db` | مسیر دیتابیس روی Volume |

> عکس/ویدیوهای منوها هم اختیاری از Variables ست می‌شوند (`LEADERBOARD_PHOTO_ID`, `SCARY_PHOTO_ID`, `PROMO_WELCOME_NEW_PHOTO_ID`, `PROMO_WELCOME_OLD_PHOTO_ID`, `REVIEW_PHOTO_ID`, `VIP_MAIN_PHOTO_ID`, `BUY_VIP_PHOTO_ID`, `LEVEL_UP_PHOTO_ID`, `LINK_TUTORIAL_VIDEO_ID`). اگر خالی باشند، ربات همان بخش را فقط با متن می‌فرستد و خطایی نمی‌دهد.

> اگر پروژه جدید ساختید، همین ۴ متغیر را بسازید + یک Volume با مسیر `/data`.

---

## 🆕 تغییرات نسخه ۳ (رفع باگ‌های گزارش‌شده)

### ۱. خطای 404 در /restore
کتابخانه telebot فایل‌ها را از `api.telegram.org` دانلود می‌کرد (نه سرور بله!) → خطای 404 Not Found.
**رفع شد:** `apihelper.FILE_URL = "https://tapi.bale.ai/file/bot{0}/{1}"`

### ۲. نمایش نشدن عکس منوها
با فعال شدن parse_mode=Markdown، نام کاربران دارای `_` یا `*` باعث خطای parse می‌شد → ربات بی‌صدا عکس را کنار می‌گذاشت و فقط متن می‌فرستاد.
**رفع شد:** تابع `escape_md` حالا `_`, `*`, `` ` `` و `\` را escape می‌کند تا کپشن‌ها هرگز نشکنند.

---

## 📱 گام‌های پس از دیپلوی

1. `/start` به ربات بدهید
2. `/restore` را بزنید و فایل `bot_data.db` قبلی را ارسال کنید
3. تصاویر منوها (لیدربورد، VIP و...) باید نمایش داده شوند — file_id ها در config.py هستند
4. هر شب ۰۰:۰۰ ایران، بکاپ خودکار به پی‌وی ادمین می‌آید

## ⚠️ امنیت
- بعد از تأیید عملکرد، توکن Railway را از Account Settings → Tokens حذف/تعویض کنید
- توکن ربات و درگاه فقط در Variables باشند (هیچ‌وقت در کد)
